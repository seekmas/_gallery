<%= bootstrap_form_for(@field ,layout: :horizontal, label_errors: true) do |f| %>
  <% if @field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@field.errors.count, "error") %> prohibited this field from being saved:</h2>

      <ul>
      <% @field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field :key_name %>

    <%= f.text_field :value_name %>

    <%= f.text_field :order_id %>

    <%= f.collection_select :activity_id , Activity.all , :id , :subject %>

    <%= f.submit %>

<% end %>
